@page
@model ExternalLoginModel
@{ ViewData["Title"] = "NOVO USUÁRIO"; }
<section class="py-1">
    <div class="row">
        <div class="col-lg-6 col-md-8 col-sm-12">
            <h2>@ViewData["Title"]</h2>
            <h4>ASSOCIE SUA CONTA @Model.LoginProvider.</h4>
            <hr />
            <p class="text-info">
                Você foi autenticado com sucesso usando sua conta <strong>@Model.LoginProvider</strong>.
                Por favor, insira um endereço de e-mail para este site abaixo e clique no botão Registrar para concluir o login.
            </p>
            <form asp-page-handler="Confirmation" asp-route-returnUrl="@Model.ReturnUrl" method="post">
                <div class="row">
                    <div class="col-lg-6 col-md-8 col-sm-12">
                        <div asp-validation-summary="All" class="text-danger"></div>
                    </div>
                </div>
                <div class="form-group">
                    <label asp-for="Input.Email"></label>
                    <input asp-for="Input.Email" class="form-control" />
                    <span asp-validation-for="Input.Email" class="text-danger"></span>
                </div>
                <button type="submit" class="btn btn-success">REGISTRAR</button>
            </form>
        </div>
    </div>
</section>
@section Scripts { <partial name="_ValidationScriptsPartial" /> }